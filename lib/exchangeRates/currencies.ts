// مسیر فایل: lib/exchangeRates/currencies.ts
// 🆕 فایل تازه (فاز M09 — همگام‌سازی با وب، بخش «اسعار») — کپیِ عینی و بدون‌تغییرِ
// src/lib/exchangeRates/currencies.ts وب؛ دقیقاً هم‌الگو با بقیه‌ی فایل‌های «تک‌نقطه‌ی حقیقت»ی که
// از تسک ۵ فاز M00 به بعد بین دو ریپازیتوری عیناً کپی می‌شوند (categories.ts، vehicleTypes.ts،
// propertyTypes.ts و…). فهرست ثابتِ ۵ ارزِ اولویت‌دار: دالر آمریکا، کلدار پاکستان، تومان ایران،
// یورو، درهم امارات — یک تصمیمِ محصولی/ثابت، نه چیزی که قرار باشد پویا تغییر کند؛ خودِ نرخ‌های
// خرید/فروش هرکدام دینامیک و از پل موبایل می‌آیند (رجوع کنید به lib/exchangeRates/api.ts).
//
// نکته درباره‌ی IRR/PKR: منبع (سرافی.af) نرخ این دو ارز را «به‌ازای هر ۱۰۰۰ واحد» گزارش می‌کند،
// نه هر ۱ واحد. فیلد perUnit این را نشان می‌دهد — components/CurrencyConverter.tsx واقعاً باید
// نرخ را قبل از ضرب/تقسیم بر همین عدد تقسیم کند، وگرنه نتیجه‌ی محاسبه ۱۰۰۰ برابرِ واقعیت می‌شود.
export type TrackedCurrency = {
  /** کدِ ارز، دقیقاً همان کدی که پل موبایل (و منبعِ اصلیِ sarafi.af) هم استفاده می‌کند، مثل USD */
  code: string;
  nameFa: string;
  namePs: string;
  /** ایموجیِ پرچم — نیازی به فایل تصویر جداگانه ندارد و در همه‌ی دستگاه‌ها رندر می‌شود */
  flag: string;
  /** نرخِ خرید/فروش به‌ازای چند واحد از این ارز است — ۱ برای بیشتر ارزها، ۱۰۰۰ برای PKR/IRR. */
  perUnit: 1 | 1000;
};

export const TRACKED_CURRENCIES: TrackedCurrency[] = [
  { code: 'USD', nameFa: 'دالر آمریکا', namePs: 'امریکايي ډالر', flag: '🇺🇸', perUnit: 1 },
  { code: 'PKR', nameFa: 'کلدار پاکستان (هزار)', namePs: 'پاکستاني کلدار (زره)', flag: '🇵🇰', perUnit: 1000 },
  { code: 'IRR', nameFa: 'تومان ایران (هزار)', namePs: 'ایراني تومان (زره)', flag: '🇮🇷', perUnit: 1000 },
  { code: 'EUR', nameFa: 'یورو', namePs: 'یورو', flag: '🇪🇺', perUnit: 1 },
  { code: 'AED', nameFa: 'درهم امارات', namePs: 'اماراتي درهم', flag: '🇦🇪', perUnit: 1 },
];

// 🛠️ افزوده‌شده (بازبینیِ کاملِ فاز M09 — تاییدِ نهاییِ همگام‌سازی): این تابع در نسخه‌ی وب
// (src/lib/exchangeRates/currencies.ts) هست ولی در کپیِ اولیه‌ی موبایل جا افتاده بود — بدون اثرِ
// عملکردی (هیچ‌جای موبایل هنوز صدایش نمی‌زد)، فقط برای هم‌ترازیِ کاملِ این فایلِ «تک‌نقطه‌ی
// حقیقت» با نسخه‌ی وب اضافه شد.
export function getTrackedCurrencyCodes(): string[] {
  return TRACKED_CURRENCIES.map((c) => c.code);
}